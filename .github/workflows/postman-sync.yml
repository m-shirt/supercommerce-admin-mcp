name: Manual Postman Sync

on:
  workflow_dispatch:
  # Only trigger on master if it's a manual push (not from postman-updates branch)
  push:
    paths:
      - 'postman/collections/*.json'
    branches: [master, main]

jobs:
  sync:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
    - uses: actions/checkout@v4
      with:
        token: ${{ secrets.GITHUB_TOKEN }}

    - uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'

    - run: npm ci

    - name: Generate tools
      id: generate
      run: |
        node scripts/generate-tools-from-postman.js
        if [ -n "$(git status --porcelain)" ]; then
          echo "changes=true" >> $GITHUB_OUTPUT
        else
          echo "changes=false" >> $GITHUB_OUTPUT
        fi

    - name: Commit changes
      if: steps.generate.outputs.changes == 'true'
      run: |
        git config user.name "GitHub Actions"
        git config user.email "actions@github.com"
        git add .
        git commit -m "chore: sync MCP tools from Postman collection"
        git push